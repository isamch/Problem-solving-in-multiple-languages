// Exercise: Binary to Decimal

// Description: Convert a binary string to decimal.

function binToDec(bin) {
  return parseInt(bin, 2);
}

// Test the function
console.log(binToDec('1010')); // Output: 10
