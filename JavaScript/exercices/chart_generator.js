// Exercise: Chart Generator

// Description: Render chart using a library like Chart.js.

function generateChart(ctx, data) {
  new Chart(ctx, { type: 'bar', data });
}

// Test the function
// generateChart(canvasContext, chartData);
