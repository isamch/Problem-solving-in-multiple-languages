// Exercise: Repeat String

// Description: Repeat a string a given number of times.

function repeatString(str, times) {
  return str.repeat(times);
}

// Test the function
console.log(repeatString('abc', 3)); // Output: 'abcabcabc'
